# dotfiles

Managed with [chezmoi](https://www.chezmoi.io/).

Currently tracked:

- `~/.config/ghostty/config`
- `~/.config/starship.toml`

Bootstrap on a new machine:

```sh
chezmoi init <github-user>/<repo>
chezmoi apply
```
